import { describe, test } from 'vitest'

describe('element assigned to variable', () => {})

describe('element passed to function', () => {})

describe('root element of component return', () => {})

describe('children of intrinsic element', () => {
  test('wrap with render call', () => {})
})

describe('children of composite element', () => {
  test('dont wrap with render call', () => {})
})

describe('jsx element', () => {
  test('is a function', () => {})
})

describe('jsx child expression', () => {
  test('always wrapped with render', () => {})
})

describe('React.cloneElement', () => {})

describe('React.isValidElement', () => {})

describe('React.Children', () => {})

describe('React.Fragment', () => {})
